<!DOCTYPE html>
<html lang="de">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta name="description" content="{{ description or 'Sportunion Rudmanns - Hobby-Liga-Fußballverein aus Stift Zwettl. Seit 1988.' }}">
  <meta name="theme-color" content="#FF6600">
  
  <title>{{ title }} | SU Rudmanns</title>
  
  <!-- Fonts -->
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  
  <!-- Styles -->
  <link rel="stylesheet" href="/css/style.css">
  
  <!-- Favicon -->
  <link rel="icon" type="image/svg+xml" href="/assets/favicon.svg">
  
  <!-- Open Graph -->
  <meta property="og:title" content="{{ title }} | SU Rudmanns">
  <meta property="og:description" content="{{ description or 'Sportunion Rudmanns - Hobby-Liga-Fußballverein' }}">
  <meta property="og:type" content="website">
  <meta property="og:url" content="{{ site.url }}{{ page.url }}">
  <meta property="og:image" content="{{ site.url }}/assets/og-image.jpg">
</head>
<body>
  <div class="page-wrapper">
    {% include "header.njk" %}
    
    <main class="main-content">
      {{ content | safe }}
    </main>
    
    {% include "footer.njk" %}
  </div>
  
  <style>
    .page-wrapper {
      display: flex;
      flex-direction: column;
      min-height: 100vh;
    }
    
    .main-content {
      flex: 1;
    }
  </style>
</body>
</html>
